import * as form from "../Form";
import styled from "styled-components";

export const LoginForm = form.Form;

export const LoginFormHeader = form.FormHeader;

export const LoginFormErrors = form.FormErrors;

export const LoginFormBody = form.FormBody;
export const LoginFormFieldGroup = form.FormFieldGroup;

export const LoginFormButton = form.FormButton;

export const TogglerContainer = styled.div`
  position: absolute;

  right: 10px;
  top: 135px;
`;
